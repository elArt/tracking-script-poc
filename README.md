## Install
npm install

## Commands
npm run build   — build dist/tracker.js
npm test        — run unit tests
npm run serve   — start local server at http://localhost:3000


## Important
Events won't be sent when opening index.html directly via file://.
Always use local server:   npm run serve
Then open http://localhost:3000
